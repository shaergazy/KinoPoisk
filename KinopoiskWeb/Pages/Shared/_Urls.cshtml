@* ReSharper disable Mvc.ActionNotResolved *@
<script type="text/javascript">

var Urls = Urls || {};

// Person
    Urls.Person = {
        GetById: '@Url.Action("Index", "People", new { handler = "ById" })',
        GetPeople: '@Url.Page("/People/Index", new { handler = "People" })',
        GetActors: '@Url.Page("/People/Index", new { handler = "Actors" })',
        GetDirectors: '@Url.Page("/People/Index", new { handler = "Directors" })',
    };

    Urls.Country = {
        GetById: '@Url.Action("Index", "People", new { handler = "ById" })',
        GetCountries: '@Url.Page("/Countries/Index", new { handler = "Countries" })',
    };
    
    Urls.Genre = {
        GetById: '@Url.Action("Index", "Genres", new { handler = "ById" })',
        GetAll: '@Url.Action("Index", "Genres")',
        GetGenres: '@Url.Page("/Genres/Index", new { handler = "Genres" })',
    };

    Urls.Movie = {
        GetAll: '@Url.Action("Index", "Movies")',
        GetPDF: '@Url.Action("Index", "Movies", new { handler = "GeneratePDF" })',
        GetExcel: '@Url.Action("Index", "Movies", new { handler = "GenerateExcel" })',
        LoadComments: '@Url.Action("Details", "Movies", new { handler = "LoadComments" })',
        Rate: '@Url.Action("Rate", "Movies", new { handler = "Rate" })',
    };

    Urls.Account = {
        Logout: '@Url.Page("/Account/Logout", new { handler = "Logout" })',
    };
</script>
