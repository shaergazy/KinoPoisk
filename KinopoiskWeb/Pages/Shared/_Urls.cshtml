<script type="text/javascript">
    var Urls = Urls || {};

    Urls.Person = {
        GetById: '@Url.Page("/People/Index", new { handler = "ById" })',
        GetPeople: '@Url.Page("/People/Index", new { handler = "People" })',
        GetActors: '@Url.Page("/People/Index", new { handler = "Actors" })',
        GetDirectors: '@Url.Page("/People/Index", new { handler = "Directors" })',
    };

    Urls.Country = {
        GetById: '@Url.Page("/Countries/Index", new { handler = "ById" })',
        GetCountries: '@Url.Page("/Countries/Index", new { handler = "Countries" })',
    };

    Urls.Genre = {
        GetById: '@Url.Page("/Genres/Index", new { handler = "ById" })',
        GetAll: '@Url.Page("/Genres/Index")',
        GetGenres: '@Url.Page("/Genres/Index", new { handler = "Genres" })',
    };

    Urls.Movie = {
        GetAll: '@Url.Page("/Movies/Index")',
        GetPDF: '@Url.Page("/Movies/Index", new { handler = "GeneratePDF" })',
        GetExcel: '@Url.Page("/Movies/Index", new { handler = "GenerateExcel" })',
        LoadComments: '@Url.Page("/Movies/Details", new { handler = "LoadComments" })',
        Rate: '@Url.Page("/Movies/Details", new { handler = "Rate" })',
        AddComment: '@Url.Page("/Movies/Details", new { handler = "AddComment" })',
        Import: '@Url.Page("/Movies/Import")',
        Search: '@Url.Page("/Movies/Import", new { handler = "Search" })',
        SearchById: '@Url.Page("/Movies/Import", new { handler = "SearchById" })',
    };

    Urls.Account = {
        Logout: '@Url.Page("/Account/Logout", new { handler = "Logout" })',
    };

    Urls.Subscription = {
        GetSubscriptionPlans: '@Url.Page("/Subscriptions/SubscriptionPlan", new { handler = "Plans" })',
    };
</script>
