@* ReSharper disable Mvc.ActionNotResolved *@
<script type="text/javascript">

var Urls = Urls || {};

// Person
    Urls.Person = {
        GetById: '@Url.Action("Index", "People", new { handler = "ById" })'
    };

    Urls.Country = {
        GetById: '@Url.Action("Index", "People", new { handler = "ById" })'
    };
    
    Urls.Genre = {
        GetById: '@Url.Action("Index", "Genres", new { handler = "ById" })',
        GetAll: '@Url.Action("Index", "Genres")'
    };

    Urls.Movie = {
        GetCountries: '@Url.Action("Create", "Movies", new { handler = "Countries" })',
        GetGenres: '@Url.Action("Create", "Movies", new { handler = "Genres" })',
        GetPeople: '@Url.Action("Create", "Movies", new { handler = "People" })',
        // GetAll: '@Url.Action("Index", "Genres")'
    };
</script>
